# ReSukiSU - explicitly disabled for the normal umi kernel variant.
#
# ReSukiSU's Kconfig defaults CONFIG_KSU to y, so the normal build
# must explicitly disable it when the ReSukiSU source is present.
# CONFIG_KSU is not set
